export enum Status {
  Active = 'active',
  Inactive = 'inactive',
}

export enum EmailActions {
  FORGOT_PASSWORD_EMAIL = 'FORGOT_PASSWORD_EMAIL',
}

export enum UserRole {
  User = 'user',
  Admin = 'admin',
}
